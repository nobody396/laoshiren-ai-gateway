# Service status does not control routing

Service Status, Channel Monitoring, and intelligent routing share Reliability Observations, but a status change never performs a Routing Action. Real traffic changes remain explicit, audited, separately authorised, and independently reversible so that a display error or manual status override cannot silently alter customer routing.
