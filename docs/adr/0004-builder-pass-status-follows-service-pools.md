# Builder Pass status follows service pools, not commercial tiers

Builder Pass publishes GPT, Claude, and Grok as separate Status Products rather than separate Plus, Pro, and Max rows because commercial quota and price tiers share the same upstream service pool within each model family. A tier becomes a separate Status Product only if its actual service path later diverges.
