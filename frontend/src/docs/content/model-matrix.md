# 模型能力矩阵

模型能力矩阵页面由矩阵生成，不手写维护。它汇总每个模型已通过验证的协议与推理强度。

## 模型能力矩阵怎么读

每一行是一个模型：

1. 模型 ID；
2. 四个协议列：Responses、Chat Completions、Messages、GenerateContent；
3. 模型原生推理强度；
4. 上下文窗口。

协议列中 ★ 表示推荐的首选接入方式；✓ 表示该模型在此协议上通过了真实请求闭环；✗ 表示不支持，包含实测负向和未在该协议上开放两种情况，鼠标悬停可看具体原因。

客户端对应的协议与推理强度总表见[客户端能力矩阵](client-matrix)。
