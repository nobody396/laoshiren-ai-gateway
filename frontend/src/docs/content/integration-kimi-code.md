# Kimi Code

**协议：** OpenAI 兼容

**配置状态：** 即将支持

Kimi Code 使用 `~/.kimi-code/config.toml` 管理 Provider 和模型。老实人AI 的一次性配置、备份、回滚和真实系统测试尚未完成，因此当前不提供未验证命令。

发布前必须完成：

1. Kimi 可用方案 Key 与实时模型目录绑定。
2. Windows PowerShell 与 macOS / Linux 配置脚本。
3. 已安装和未安装两种场景。
4. 最小请求、重复运行和回滚验证。

在状态改为“已验证”前，不要手工修改客户配置文件。
