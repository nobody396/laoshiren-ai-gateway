# 模型目录

模型目录由实时接口生成。同一个模型可能由多个分组提供，请根据预算选择对应分组。

## 分组怎么选

- 按量分组按实际调用计费，适合用量不固定的用户。
- 月卡只有 Plus、Pro、Max 三档。购买后会同时开放对应的 GPT、Claude 和 Grok 月卡分组，三类分组共用同一份 31 天额度。
- 创建 Key 时选择准备使用的模型系列和月卡档位。每把 Key 只使用自己所选分组的模型、倍率和额度。
- 当前价格、额度和可用模型以下方实时目录及购买页为准。
