# ZCode

**协议：** Anthropic Messages

**推荐模型：** GLM

**配置状态：** 实验支持

本机已确认 ZCode CLI 支持模型、Base URL 和 API Key 配置，但 ZCode 桌面端与 CLI 使用不同配置入口。完整的一次性配置和真实系统 E2E 尚未完成，因此当前不提供未验证命令。

发布前必须完成：

1. GLM 可用方案 Key 接入 ZCode CLI。
2. ZCode 桌面端正式配置入口核对。
3. Windows PowerShell 与 macOS / Linux 验收。
4. 模型调用、工具使用、重复运行和回滚验证。

在状态改为“已验证”前，不要直接编辑 ZCode 内部状态文件。
