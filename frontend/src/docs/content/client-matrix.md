# 客户端能力矩阵

客户端能力矩阵页面由矩阵生成，不手写维护。它汇总每个客户端已通过验证的协议、推理强度控制和一键配置状态。

## 客户端能力矩阵怎么读

每一行是一个客户端：

1. 客户端名称与精确版本；
2. 已验证协议——通过真实 Agent 闭环的协议；
3. 推理强度控制方式；
4. 一键配置是否可用。

协议列中 ✓ 表示已通过真实 Agent 闭环；✗ 表示实测不支持；○ 表示未验证、还没跑过闭环。只有带 ✓ 协议的客户端才会生成写入命令。

模型对应的协议与推理强度总表见[模型能力矩阵](model-matrix)。
